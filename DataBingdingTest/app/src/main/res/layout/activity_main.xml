<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.xkf.databingdingtest.MainViewModel" />

        <variable
            name="viewModel"
            type="MainViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        tools:context=".MainActivity">

        <CheckBox
            android:id="@+id/checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="@={viewModel.checked}"
            android:text="@{viewModel.checkText}"
            tools:text="这是一个复选框" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@{viewModel.title}"
            android:textColor="@{viewModel.titleColor}"
            android:textSize="@{viewModel.titleSize}"
            tools:text="这是一个大标题" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@{viewModel.subTitle}"
            android:textColor="@{viewModel.subTitleColor}"
            android:textSize="@{viewModel.subTitleSize}"
            tools:text="这是一个小标题" />
    </LinearLayout>
</layout>